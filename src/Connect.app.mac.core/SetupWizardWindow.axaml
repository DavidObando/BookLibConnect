<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:BookLibConnect.Core.UI.Mac.ViewModels"
        xmlns:views="using:BookLibConnect.Core.UI.Mac.Views"
        x:Class="BookLibConnect.App.Mac.SetupWizardWindow"
        x:DataType="vm:ProfileWizardViewModel"
        Title="Book Lib Connect — Setup Wizard"
        Width="650" Height="550"
        MinWidth="550" MinHeight="450"
        WindowStartupLocation="CenterOwner"
        CanResize="False">

  <views:ProfileWizardView DataContext="{Binding}" />

</Window>
